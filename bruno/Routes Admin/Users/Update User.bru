meta {
  name: Update User
  type: http
  seq: 4
}

patch {
  url: http://localhost:3001/v1/admin/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
  Content-Type: application/json
}

body:json {
  {
    "id": 1,
    "firstName": "Updated",
    "lastName": "User"
  }
}

vars:pre-request {
  adminToken: 
}

docs {
  # Update User (Admin)
  
  Met à jour un utilisateur (admin uniquement).
}

