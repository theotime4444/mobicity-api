Pour la base de données il faut installer postgres une fois installer dans le stack builder il faut installer postGIS

Pour le projet il faut faire un npm install pour installer toutes les dépendances venant du package.json

